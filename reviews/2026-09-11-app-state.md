# Omni-Reviewer — App state

**Date:** 2026-09-11  
**HEAD at review start:** `594f97e` (*Complete secure study workflows and learning tools*)

## Current status

Invite-only personal study desk. Topics hold packs; packs hold private sources and four persisted study modes (Locked In, Summary, Test Me, Carded). Generate and Redo are explicit. Tabs never call a model.

OpenRouter is the live generation path for all four modes and for image-source vision. Primaries are pinned `:free` ids. `openrouter/auto` is rejected. Gemini is gone.

The 2026-09-11 ops pass is in this worktree, ready to commit on `main`:

| Item | State |
| --- | --- |
| Next | 16.3.4 (`eslint-config-next` 16.3.4; transitive sharp 0.35.4) |
| `.env.example` | Restored as empty placeholders only. Real env files stay ignored |
| `vercel.json` | `maxDuration` 300 on `generation/[jobId]/route.ts` |
| CI | `.github/workflows/test.yml` runs `npm test` and `npm run lint` on main/PRs |
| Login throttle | Email-keyed, 5 failures / 15 minutes, 15-minute lock |
| Default tests | 178 passed, 8 skipped |
| Lint / tsc / build | Pass on Next 16.3.4 |

Auth remains email/password, scrypt, no public signup. Ingest remains text PDF, DOCX, PPTX, image, text, paste. Video/audio stored only. Scanned PDF still disabled.

Live: https://omni-reviewer.vercel.app (not re-checked this session).

## Open items

### Done this pass

1. Restored `.env.example` from git HEAD placeholders (`AUTH_TRUST_HOST=true` is the only non-empty assignment). Removed the `.gitignore` `.env*` blanket. `.env` / `.env.local` still ignored.
2. Bumped Next and `eslint-config-next` to 16.3.4. drizzle-kit left at 0.31.10.
3. Pointed `vercel.json` `maxDuration` at the generation job route.
4. Added GitHub Actions CI with no secrets.
5. Durable login throttle: `login_throttles` table, lock check before `signIn`, generic misconfigured copy on query failure.

### Operator follow-up

- `npm run db:push` on Neon direct/unpooled URL so `login_throttles` exists in production. Until that runs, login throttle queries fail closed with "Server is misconfigured. Try again later."
- Confirm Production/Preview pick up Next 16.3.4 and the job-route duration.

### Deferred (v1)

- Scanned-PDF vision until a bounded page renderer exists
- Audio / video transcription
- YouTube ingest
- Public signup
- Committed Drizzle migration history (`db:push` stays the operator path)

## Things that need to be done

Nothing else in the study-mode product for this round. Next work is operator `db:push` / deploy pickup, then the deferred ingest list if a Wave 5 is opened.
